<section class="hero-container">
	<div class="hero-body">
		<!-- {%- if section.settings.hero_image != blank -%}
		<div class="hero-image-container">
			<img
				srcset="{%- if section.settings.hero_image.width >= 375 -%}{{ section.settings.hero_image | img_url: '375x' }} 375w,{%- endif -%}
          {%- if block.settings.hero_image.width >= 550 -%}{{ block.settings.hero_image | img_url: '550x' }} 550w,{%- endif -%}
          {%- if section.settings.hero_image.width >= 750 -%}{{ section.settings.hero_image | img_url: '750x' }} 750w,{%- endif -%}
          {%- if section.settings.hero_image.width >= 1100 -%}{{ section.settings.hero_image | img_url: '1100x' }} 1100w,{%- endif -%}
          {%- if section.settings.hero_image.width >= 1500 -%}{{ section.settings.hero_image | img_url: '1500x' }} 1500w,{%- endif -%}
          {%- if section.settings.hero_image.width >= 1780 -%}{{ section.settings.hero_image | img_url: '1780x' }} 1780w,{%- endif -%}
          {%- if section.settings.hero_image.width >= 2000 -%}{{ section.settings.hero_image | img_url: '2000x' }} 2000w,{%- endif -%}
          {%- if section.settings.hero_image.width >= 3000 -%}{{ section.settings.hero_image | img_url: '3000x' }} 3000w,{%- endif -%}
          {%- if section.settings.hero_image.width >= 3840 -%}{{ section.settings.hero_image | img_url: '3840x' }} 3840w,{%- endif -%}
          {{ section.settings.hero_image | img_url: 'master' }} {{ section.settings.hero_image.width }}w"
				sizes=""
				src="{{ section.settings.hero_image | img_url: '1500x' }}"
				loading="lazy"
				alt="{{ section.settings.hero_image.alt | escape }}"
				width="{{ section.settings.hero_image.width }}"
				height="{{
					section.settings.hero_image.width
						| divided_by: section.settings.image.aspect_ratio
				}}"
			/>
		</div>
		{%- endif -%} -->
    {% if section.settings.hero_image_movil != blank or section.settings.hero_image_desktop != blank %}
      <picture>
        <source media="(min-width:767px)" srcset="{{ section.settings.hero_image_desktop | img_url: 'master' }}">
        <img
          src="{{ section.settings.hero_image_movil | img_url: 'master' }}"
          alt="{{ section.settings.hero_image_movil.alt }}"
        >
      </picture>
      <div class="btn-header">
        <a href="{{ section.settings.url_poduct }}" >{{ section.settings.text_button }}</a>
      </div>
    {% endif %}
	</div>


</section>


{% schema %}

{
  "name": "Section Hero",
  "settings": [
    {
      "type": "image_picker",
      "id": "hero_image_movil",
      "label": "Inserte Imagen del baner Para Tamaño Movil"
    },
    {
      "type": "image_picker",
      "id": "hero_image_desktop",
      "label": "Inserte Imagen del baner Para Tamaño Laptop"
    },
    {
      "type": "text",
      "id": "text_button",
      "label": "Texto Boton",
      "default": "Comprar Ahora"
    },
    {
      "type": "url",
      "id": "url_poduct",
      "label": "URL destino"
    }
  ],
  "presets": [
		{
			"name": "Hero"
		}
	]
}

{%
endschema %}
